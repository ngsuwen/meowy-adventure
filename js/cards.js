// combat cards: attack and defense
// magic cards: buffs, debuffs, heal, special effect
// play function: remove from hand, effect

const cardList = [{
    sn: 0,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 1,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 2,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 3,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 4,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 5,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 6,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 7,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 8,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 9,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 10,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 11,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 12,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  },
  {
    sn: 13,
    play: function(player, against) {
      // player.action(cardList[0].sn)
      // against.takeDmg(2)
      console.log(this.sn)
    }
  }
]